import ITask from '../models/task/types/task.type';

const task: ITask[] = [
  {
    id: '1',
    subject: 'Npm run Mael',
    projectId: '1',
    startDate: new Date(),
    endDate: new Date(),
    estimeeSpentTime: 5,
    advancement: 'DONE',
  },
];

export default task;
