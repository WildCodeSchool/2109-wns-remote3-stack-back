import ITask from 'src/models/task/types/task.type';

const task: ITask[] = [
  {
    id: '1',
    subject: 'Npm run Mael',
    projectId: '1',
    startDate: new Date(),
    endDate: new Date(),
  },
];

export default task;
